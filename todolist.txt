# Scraper - Should this be timed ?? or manual ??
# Models - how will the models be strcutured, in terms of teams, years + data
# What addititional info can be added from my other repo
# Add django rest framework for api, to post and update
# Move minutes model to api models or create main app
# Can I make the API avaialable via a key -> user registers, key generated, sent via email, authentication of key in headers.

# League model - > league name - > short_code -> id

# Maybe player database (figure how this will work)
# Players info to create player charts (like on bot)

1. Figure out page navigation
2. Add css https://www.w3schools.com/django/django_add_css_file.php